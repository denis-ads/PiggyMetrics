spring:
  profiles.active: dev
  application:
    name: account-service
  
---
spring:
  profiles: dev
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      password: ABC123
      username: user

---
spring:
  profiles: hom
  cloud:
    config:
      uri: http://10.220.42.26:8888
      fail-fast: true
      password: ABC123
      username: user
      
---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://config:8888
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user